NODE_ENV=production
PORT=8080
DATA_DIR=/data
# Attach a Railway Volume at /data. Railway sets RAILWAY_VOLUME_MOUNT_PATH automatically.
# Generate the following with: node scripts/create-secrets.mjs
TEAM_PASSWORD_HASH=
ADMIN_PASSWORD_HASH=
SESSION_SECRET=
SYNC_TOKEN=
# Local preview only (binds to 127.0.0.1, forbidden in production):
# DEMO_MODE=true
