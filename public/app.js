const $=s=>document.querySelector(s);
let data=null, demo=false, generation=0;
const escape=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const date=s=>s?new Intl.DateTimeFormat('nl-NL',{day:'numeric',month:'short',year:'numeric',timeZone:'Europe/Amsterdam'}).format(new Date(s)):'';
const statusClass=s=>s==='Actie nodig'?'action':s==='Op schema'?'good':'';
const statusHTML=s=>`<span class="status ${statusClass(s)}">${escape(s)}</span>`;
function text(value){return escape(value||'Niet ingevuld');}
function periodName(label){const value=String(label||'');return escape(value.length>22?value.slice(0,20)+'…':value);}
function priorityClass(value){return value!==null&&value<=3?'top':'';}
function renderPriorityHistory(){
  const target=$('#priorityHistory'),history=data?.priorityHistory;
  if(!history?.periods?.length){target.innerHTML='<div class="empty">Nog geen vastgelegde prioriteitsmomenten.</div>';return;}
  target.innerHTML=`<table class="priority-table"><thead><tr><th>Klant</th>${history.periods.map(p=>`<th title="${escape(p.label)}">${periodName(p.label)}</th>`).join('')}<th>Verschuiving</th></tr></thead><tbody>${history.rows.map(row=>`<tr><th>${escape(row.name)}</th>${row.positions.map(value=>`<td>${value===null?'<span class="priority-missing">–</span>':`<span class="priority-dot ${priorityClass(value)}">${value}</span>`}</td>`).join('')}<td>${row.movement===null?'<span class="movement neutral">Nog geen vergelijking</span>':row.movement===0?'<span class="movement neutral">Gelijk</span>':`<span class="movement ${row.movement>0?'up':'down'}">${row.movement>0?'↑':'↓'} ${Math.abs(row.movement)} plek${Math.abs(row.movement)===1?'':'ken'}</span>`}</td></tr>`).join('')}</tbody></table>`;
}
async function api(path,options){const r=await fetch(path,options);const j=await r.json();if(!r.ok){if(r.status===401&&path!='/api/login')showLogin();throw Error(j.error||'Gegevens konden niet worden geladen.');}return j;}
function showLogin(){$('#app').classList.add('hidden');$('#login').classList.remove('hidden');}
function showApp(){$('#app').classList.remove('hidden');$('#login').classList.add('hidden');$('#logout').classList.toggle('hidden',demo);}
function changeHTML(c){const x=c.change;if(x.kind==='unknown')return '<span class="change">Niet vastgelegd<small>Eerdere prioriteit onbekend</small></span>';if(x.kind==='same')return '<span class="change">Gelijk<small>Zelfde positie</small></span>';return `<span class="change ${x.kind}">${x.kind==='up'?'↑':'↓'} ${escape(x.from)} naar ${escape(x.to)}<small>${x.kind==='up'?'Hogere':'Lagere'} prioriteit</small></span>`;}
function historyHTML(h){return `<div class="history-item"><div class="history-meta"><strong>${escape(h.label)}</strong><span>${h.priority===null?'Prioriteit niet vastgelegd':'Prioriteit '+h.priority}</span>${statusHTML(h.status)}${h.sourceWeek&&h.reportWeek&&h.sourceWeek!==h.reportWeek?`<span>Laatste actie uit week ${h.sourceWeek}</span>`:''}</div><div class="history-copy"><p><b>Laatste actie</b><br>${text(h.lastAction)}</p><p class="old-next"><b>Openstaande actie toen</b><br>${text(h.nextAction)}</p>${h.changeReason?`<p><b>Toelichting</b><br>${text(h.changeReason)}</p>`:''}</div></div>`;}
function renderClients(){if(!data)return;const open=new Set([...document.querySelectorAll('.client[open]')].map(e=>e.dataset.id));const q=$('#search').value.trim().toLocaleLowerCase('nl'),status=$('#status').value,channel=$('#channel').value;const filtered=data.clients.filter(c=>(!q||[c.name,c.lastAction,c.nextAction].join(' ').toLocaleLowerCase('nl').includes(q))&&(!status||c.status===status)&&(!channel||c.channel.includes(channel))&&(!$('#onlyChanges').checked||['up','down'].includes(c.change.kind)));$('#resultCount').textContent=filtered.length;$('#clients').innerHTML=filtered.length?filtered.map(c=>`<details class="client" data-id="${escape(c.id)}" ${open.has(c.id)?'open':''}><summary class="client-summary"><span class="priority ${c.priority!==null&&c.priority<=3?'top':''}" aria-label="Prioriteit ${c.priority??'onbekend'}">${c.priority??'?'}</span><span><strong class="client-name">${escape(c.name)}</strong><span class="channel">${escape(c.channel||'Kanaal niet ingevuld')}</span></span>${statusHTML(c.status)}<span class="summary-action summary-last"><span class="action-label">Laatste actie</span><span class="next-action">${text(c.lastAction)}</span></span><span class="summary-action summary-next"><span class="action-label">Openstaande actie</span><span class="next-action">${text(c.nextAction)}</span>${c.deadline?`<span class="deadline">Deadline ${date(c.deadline)}</span>`:''}</span>${changeHTML(c)}<span class="chevron" aria-hidden="true">›</span></summary><div class="client-body"><div class="detail-grid"><div><h3>LAATSTE ACTIE</h3><p>${text(c.lastAction)}</p></div><div><h3>OPENSTAANDE ACTIE</h3><p>${text(c.nextAction)}</p>${c.deadline?`<p class="muted">Deadline: ${date(c.deadline)}${c.deadlineType?' · '+escape(c.deadlineType):''}</p>`:''}</div>${c.risk?`<div class="attention"><h3>AANDACHTSPUNT</h3><p>${text(c.risk)}</p></div>`:''}${c.changeReason?`<div><h3>WAAROM DE PRIORITEIT VERANDERDE</h3><p>${text(c.changeReason)}</p></div>`:''}${c.sourceWeek&&data.selected.reportWeek&&c.sourceWeek<data.selected.reportWeek?`<div><h3>LAATSTE KLANTUPDATE</h3><p>Week ${c.sourceWeek}. Geen nieuwe klantupdate in de geselecteerde week.</p></div>`:''}</div><section class="history" aria-label="Historie van ${escape(c.name)}"><div class="history-title"><strong>Eerdere updates</strong><span>Nieuwste eerst · ${c.history.length} updates</span></div>${c.history.length?c.history.slice(0,3).map(historyHTML).join(''):'<div class="empty">Nog geen eerdere updates voor deze klant.</div>'}${c.history.length>3?`<div class="hidden older-history">${c.history.slice(3).map(historyHTML).join('')}</div><button class="history-more text-button" aria-expanded="false">Alle ${c.history.length} updates bekijken</button>`:''}</section></div></details>`).join(''):'<div class="empty">Geen klanten met deze filters. Pas je zoekopdracht of filters aan.</div>';}
async function load(snapshot='',compare=''){
  const token=++generation;$('#error').classList.add('hidden');
  try{const next=await api('/api/dashboard?'+new URLSearchParams({snapshot,compare}));if(token!==generation)return;data=next;
    $('#snapshot').innerHTML=data.snapshots.map(s=>`<option value="${escape(s.id)}">${escape(s.label)}</option>`).join('');
    if(!data.selected){$('#updateLabel').textContent='Nog geen update ontvangen';$('#clients').innerHTML='<div class="empty">De eerste update uit Google Sheets verschijnt hier zodra de koppeling actief is.</div>';$('#sourceNotice').classList.add('hidden');return;}
    $('#snapshot').value=data.selected.id;const selectedIndex=data.snapshots.findIndex(s=>s.id===data.selected.id);const options=data.snapshots.slice(selectedIndex+1);$('#compare').innerHTML=options.length?options.map(s=>`<option value="${escape(s.id)}">${escape(s.label)}</option>`).join(''):'<option>Geen eerdere update</option>';$('#compare').disabled=!options.length;if(data.comparison)$('#compare').value=data.comparison.id;
    $('#updateLabel').textContent=data.selected.kind==='published'?'Vastgelegd: '+data.selected.label:'Bestandsimport · '+data.selected.label;
    $('#periodBadge').textContent=selectedIndex===0?(data.selected.kind==='published'?'Laatste update':'Startstand'):'Eerdere update';
    const notice=$('#sourceNotice');notice.classList.toggle('hidden',data.selected.kind==='published');notice.textContent=data.selected.kind==='baseline'?'Startstand uit je aangeleverde bestand. De eerdere weken bevatten geen opgeslagen prioriteiten. Nieuwe vastgelegde updates maken de vergelijking mogelijk.':'Historische week uit het aangeleverde bestand. Het exacte verzendmoment en de toenmalige prioriteiten zijn niet vastgelegd.';
    $('#total').textContent=data.clients.length;$('#navCount').textContent=data.clients.length;$('#needsAction').textContent=data.clients.filter(c=>c.status==='Actie nodig').length;$('#onTrack').textContent=data.clients.filter(c=>c.status==='Op schema').length;
    const known=data.clients.filter(c=>c.change.kind!=='unknown');$('#changed').textContent=known.length?known.filter(c=>c.change.kind!=='same').length:'?';$('#changeCaption').textContent=known.length?`Van ${known.length} vergelijkbare klanten`:'Eerdere prioriteiten ontbreken';renderPriorityHistory();renderClients();
  }catch(e){$('#error').textContent=e.message;$('#error').classList.remove('hidden');}
}
$('#loginForm').addEventListener('submit',async e=>{e.preventDefault();const button=e.target.querySelector('button');button.disabled=true;$('#loginError').textContent='';try{await api('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:$('#password').value})});$('#password').value='';showApp();await load();}catch(e){$('#loginError').textContent=e.message;}finally{button.disabled=false;}});
$('#logout').addEventListener('click',async()=>{try{await api('/api/logout',{method:'POST'});data=null;$('#clients').innerHTML='';showLogin();}catch(e){$('#error').textContent=e.message;$('#error').classList.remove('hidden');}});
$('#refresh').addEventListener('click',()=>load());$('#snapshot').addEventListener('change',()=>load($('#snapshot').value));$('#compare').addEventListener('change',()=>load($('#snapshot').value,$('#compare').value));
for(const id of ['#search','#status','#channel','#onlyChanges'])$(id).addEventListener(id==='#search'?'input':'change',renderClients);
$('#collapse').addEventListener('click',()=>document.querySelectorAll('.client[open]').forEach(e=>e.open=false));
$('#clients').addEventListener('click',e=>{const button=e.target.closest('.history-more');if(!button)return;const more=button.previousElementSibling;more.classList.toggle('hidden');const expanded=!more.classList.contains('hidden');button.setAttribute('aria-expanded',String(expanded));button.textContent=expanded?'Alleen de laatste 3 updates':'Alle updates bekijken';});
async function start(){try{const s=await api('/api/session');demo=s.demo;if(s.authenticated){showApp();await load();}else showLogin();}catch(e){showLogin();$('#loginError').textContent=e.message;}}
start();
